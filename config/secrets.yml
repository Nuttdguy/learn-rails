# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  gmail_username: <%= ENV["GMAIL_USERNAME"] %>
  gmail_password: <%= ENV["GMAIL_PASSWORD"] %>
  mailchimp_api_key: <%= ENV['MAILCHIMP_API_KEY'] %>
  mailchimp_list_id: <%= ENV['MAILCHIMP_LIST_ID'] %>
  domain_name: 'thealchemist.herokuapp.com'
  owner_email: <%= ENV["OWNER_EMAIL"] %>
  secret_key_base: e9060e371e4fa67b9a297c4e233a5fd640ea48bd9e843589f65e99db7a84edea16aa7c8b4e9d565afcf51943008ef22229a967dcf98472b5d6ce3768065503f0

test:
  secret_key_base: 5641e0d94619aa02e50cc02b7cf8e1646d82da7f1709e30d76ff7042ef23b614cf63b34b3b8aae792d4582db8e27b55887750c0fb8e1030b076b56c8434dc46d

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  gmail_username: <%= ENV["GMAIL_USERNAME"] %>
  gmail_password: <%= ENV["GMAIL_PASSWORD"] %>
  mailchimp_api_key: <% ENV["MAILCHIMP_API_KEY"] %>
  mailchimp_list_id: <% ENV["MAILCHIMP_LIST_ID"] %>
  domain_name: <%= ENV["DOMAIN_NAME"] %>
  owner_email: <%= ENV["OWNER_EMAIL"] %>
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
